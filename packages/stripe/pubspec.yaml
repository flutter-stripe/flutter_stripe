name: flutter_stripe
description: Flutter library for Stripe.
version: 1.0.1+1
homepage: https://github.com/flutter-stripe/flutter_stripe
repository: https://github.com/flutter-stripe/flutter_stripe

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter
  stripe_android: ^0.0.1+1
  stripe_ios: ^0.0.1+1
  stripe_platform_interface: ^0.0.1+1
dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.11.0




